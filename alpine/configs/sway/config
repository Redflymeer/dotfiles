# ~/.config/sway/config
set $mod Mod4
font monospace 10


# Фон
# Обои на все мониторы
exec_always --no-startup-id swaybg -m fill -i "/home/alpine/.wallpapers/knight_wallpaper7.jpg" &

# === Клавиатура ===
input type:keyboard {
    xkb_layout "us,ru"
    xkb_options "grp:win_space_toggle"
    repeat_rate 25
    repeat_delay 200
}

# === Автозапуск ===
exec --no-startup-id pipewire &
exec --no-startup-id wireplumber &

# blueman 
exec --no-startup-id blueman-applet &

# D-Bus
exec --no-startup-id dbus-daemon --session --address=unix:path=$XDG_RUNTIME_DIR/bus &
# Вэйбар
exec_always --no-startup-id waybar

# === Бинды ===
bindsym $mod+d exec wofi --show drun
bindsym Ctrl+Alt+T exec kitty

# === Бинды окна ===
bindsym $mod+Tab focus right
bindsym $mod+Shift+Tab focus left


# === Бинды ===
bindsym $mod+q kill                    # Закрыть окно
bindsym $mod+f fullscreen toggle       # Полноэкранный режим
bindsym $mod+Shift+r reload            # Перезагрузить конфиг
bindsym $mod+Shift+e exec swaymsg -t exit  # Выход из сессии
bindsym $mod+Shift+c reload            # Reload config (дубликат для удобства)

# === Навигация ===
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# === Бинды рабочие столы ===
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4

# === Стиль окон ===
default_border pixel 2 
default_floating_border pixel 2



